[package]
name = "yihu-core"
version.workspace = true
edition.workspace = true
description = "一呼共享核心库：Linux 系统信息读取"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
libc = "0.2"
