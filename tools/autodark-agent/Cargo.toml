[package]
name = "autodark-agent"
version.workspace = true
edition.workspace = true
description = "MiniTools · 自动深浅色切换执行器（无 UI，供 systemd 定时器调用）"

[dependencies]
mt-core = { path = "../../crates/mt-core" }
